# Jarvis backend — Phase 1
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.8
pydantic-settings>=2.4
asyncpg>=0.29        # Postgres on Render (SQLite is used automatically when DATABASE_URL is unset)
apscheduler>=3.10    # the heartbeat (Milestone 4)
cryptography>=42     # private-room encryption at rest (Milestones 2/6)
pypdf>=4             # document library: PDF text extraction
python-docx>=1.1     # document library: Word text extraction
boto3>=1.34          # document library: Cloudflare R2 (S3-compatible) storage
