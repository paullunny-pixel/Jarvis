# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Secrets — real keys live only in Render's Environment tab
.env

# Local dev database (SQLite, created when DATABASE_URL is unset)
jarvis.db*

# Mac desktop app — local artifacts only (the model re-downloads on install)
mac-app/node_modules/
mac-app/assets/porcupine_params.pv
mac-app/assets/*.ppn
mac-app/dist/
